# WebDecoy Netlify sensor test site

Static test site for the WebDecoy Crawler Sensor Netlify extension (WebDecoy/app #996). Deployed on Netlify; the extension injects its edge function during the (no-op) build when the three `WEBDECOY_*` environment variables are set on the site.
